# Secrets - never commit real environment values
.env
.env.*
!.env.example

# Local runtime data
*.db
*.log

# Dependencies (none currently, but standard practice if any are added)
node_modules/

# OS/editor noise
.DS_Store
