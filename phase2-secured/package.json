{
  "name": "taskforge",
  "version": "2.0.0",
  "description": "TaskForge - Phase 2 (secured) capstone build. Deliberately zero runtime dependencies to eliminate third-party supply-chain risk (see SECURITY_ANALYSIS.md and ITERATION2_SECURE_CODING.md).",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "lint:sast": "bash scripts/sast-check.sh",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {},
  "devDependencies": {}
}
